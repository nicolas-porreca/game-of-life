// With the exception of the PAUSE speed, these will be the dividers of the default 1000ms interval for the game loop
export enum GAME_SPEED {
    'PAUSE' = 0,
    'NORMAL' = 1,
    'FASTER' = 10,
    'FASTEST' = 100,
}
